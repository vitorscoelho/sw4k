package vitorscoelho.sw4k

import vitorscoelho.sw4k.comutils.*
import vitorscoelho.sw4k.comutils.dlls.LoaderJacobDll
import vitorscoelho.sw4k.sap.SapObject
import vitorscoelho.sw4k.sapenums.*
import java.text.DecimalFormat

fun main() {
    //TODO Colocar valores padrões nas funções 'get' dos objetos SAP2000, para não ter sempre que forçar o usuario a criar um objeto 'ByRef'. O ideal é que este objeto que representa o valor padrão não seja populado na chamada do método, para que nçao se gaste recurso sem ser necessário
}